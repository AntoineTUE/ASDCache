"""`readASD`, a package to lookup data from the NISt Atomic Spectra Database, utlizing caching for fast responses.

To make the most use out of the cache, `readASD` is opinionated in the information it retrieves from the ASD; it always requests the same schema of information and locally computes additional fields.
"""
